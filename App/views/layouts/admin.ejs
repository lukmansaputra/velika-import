<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard - Toko Online</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=open_in_new" />
  <link
      href="https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.snow.css"
      rel="stylesheet"
    />
  <style>
.material-symbols-outlined {
  font-variation-settings:
  'FILL' 0,
  'wght' 400,
  'GRAD' 0,
  'opsz' 24
}
</style>
</head>

<body class="bg-gray-100 overflow-x-hidden">
  <!-- Wrapper -->
  <div class="min-h-screen flex flex-col md:flex-row overflow-x-hidden">

    <!-- Sidebar -->
    <aside id="sidebar"
      class="fixed md:static z-50 inset-y-0 left-0 w-64 bg-white shadow-lg transform -translate-x-full md:translate-x-0 transition-transform duration-300">
      <div class="p-6 text-xl font-bold text-blue-600 border-b border-gray-200">
        Admin Panel
      </div>
      <nav class="p-4 space-y-2">
        <a href="/dashboard" class="block py-2 px-4 rounded hover:bg-blue-100 text-gray-700">Dashboard</a>
        <a href="/dashboard/product" class="block py-2 px-4 rounded hover:bg-blue-100 text-gray-700">Products</a>
        <a href="/dashboard/category" class="block py-2 px-4 rounded hover:bg-blue-100 text-gray-700">Categories</a>
        <br>
        <a href="/dashboard/logout" class="block py-2 px-4 rounded hover:bg-blue-100 text-gray-700 border-t">Logout</a>
        <!-- <a href="#" class="block py-2 px-4 rounded hover:bg-blue-100 text-gray-700">Orders</a>
        <a href="#" class="block py-2 px-4 rounded hover:bg-blue-100 text-gray-700">Customers</a>
        <a href="#" class="block py-2 px-4 rounded hover:bg-blue-100 text-gray-700">Settings</a> -->
      </nav>
    </aside>

    <!-- Overlay (mobile) -->
    <div id="overlay" class="fixed inset-0 bg-black bg-opacity-50 z-40 hidden md:hidden"></div>

    <!-- Main Content -->
    <div class="flex-1 flex flex-col overflow-x-hidden">
      <!-- Topbar -->
      <header class="bg-white shadow-md p-4 flex justify-between items-center md:px-6">
        <!-- Mobile Menu Button -->
        <div class="md:hidden flex items-center justify-between w-full">
          <div class="text-xl font-bold text-blue-600">Admin Panel</div>
          <button id="menu-toggle" class="text-gray-700">
            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>

        <!-- Admin Info -->
        <div class="hidden md:flex items-center gap-3 ml-auto">
          <span class="text-gray-600">Admin</span>
          <img src="https://i.pravatar.cc/40" alt="Avatar" class="rounded-full w-8 h-8" />
        </div>
      </header>

      <!-- Page Content -->
      <main class="w-full px-4 py-6 md:px-8 space-y-6 overflow-x-auto">
        <%- body %>
      </main>
    </div>
  </div>

  <!-- Script -->
  <script>
    const toggleBtn = document.getElementById('menu-toggle');
    const sidebar = document.getElementById('sidebar');
    const overlay = document.getElementById('overlay');

    toggleBtn?.addEventListener('click', () => {
      sidebar.classList.toggle('-translate-x-full');
      overlay.classList.toggle('hidden');
    });

    overlay?.addEventListener('click', () => {
      sidebar.classList.add('-translate-x-full');
      overlay.classList.add('hidden');
    });
  </script>
</body>

</html>
